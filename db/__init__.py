"""Database layer: connection setup + migrations + data load.

Engine-agnostic via a DATABASE_URL. Defaults to SQLite (zero install); point it
at PostgreSQL for a real local server. See DB-SETUP.md.
"""
