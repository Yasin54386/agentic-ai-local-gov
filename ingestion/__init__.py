"""Public open-data ingestion engine for the NT/Darwin Data Fabric.

Harvests every publicly available NT and City of Darwin dataset into one
canonical catalog (the public data repository index), and downloads the actual
data on demand. Standard-library only — no third-party dependencies.
"""

__version__ = "0.1.0"
